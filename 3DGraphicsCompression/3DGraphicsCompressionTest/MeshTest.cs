namespace _3DGraphicsCompressionTest
{
    using _3DGraphicsCompression.ObjParser;
    using Microsoft.VisualStudio.TestTools.UnitTesting;

    /// <summary>
    /// Tests the <see cref="Mesh"/> class.
    /// </summary>
    [TestClass]
    public class MeshTest
    {
        /// <summary>
        /// Tests getters of the <see cref="Mesh"/> class.
        /// </summary>
        [TestMethod]
        public void TestGetters()
        {
            /*int[] faces = { 1, 2, 3, 4 };
            double[] vertices = { 1.1, 2.2, 3.3 };
            double[] texture = { 0.1, 0.5, 0.3 };
            Mesh mesh = new Mesh(vertices, faces, texture);
            Assert.AreSame(vertices, mesh.Vertices);
            Assert.AreSame(faces, mesh.Faces);
            Assert.AreSame(texture, mesh.Texture);*/
        }
    }
}
